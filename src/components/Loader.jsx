import React from 'react';
import '../CSS files/components/Loader.css';

const Loader = () => (
  <div className="loader-container">
    <div className="loader"></div>
  </div>
);

export default Loader;
